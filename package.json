{
  "name": "inlinead-website",
  "private": true,
  "description": "INLINE AD marketing landing page",
  "scripts": {
    "dev": "netlify dev",
    "deploy": "netlify deploy --prod",
    "deploy:draft": "netlify deploy"
  },
  "devDependencies": {
    "netlify-cli": "^23.0.0"
  }
}
