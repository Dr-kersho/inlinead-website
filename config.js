// Public Supabase keys only (anon). Override locally or set GitHub Actions secrets:
// SUPABASE_URL, SUPABASE_ANON_KEY
window.INLINEAD = Object.assign(window.INLINEAD || {}, {
  supabaseUrl: '',
  supabaseAnonKey: '',
});
